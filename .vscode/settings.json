// Place your settings in this file to overwrite default and user settings.
{
	"files.exclude": {
		"build": false // set this to true to hide the "build" folder with the compiled JS files
	},
	"search.exclude": {
		"build": true
	}
}