// Place your settings in this file to overwrite default and user settings.
{
	"files.exclude": {
		"build": false, // set this to true to hide the "build" folder with the compiled JS files,
		"npm-debug.log**": true
	},
	"search.exclude": {
		"build": true
	},
	
	// Disable any JS validation to not interfer with Flow
	"javascript.validate.syntaxValidation": false,
	"javascript.validate.semanticValidation": false
}